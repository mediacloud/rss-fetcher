# packages in this file installed in BOTH dev & pre-commit venvs
# (for running to deploy, and for linting)
#
# COULD generate this via pip-compile --extras=deploy -o req-deploy.txt
# BUT while mc-deploy being developed/debugged that's a slow extra step.
mc-deploy @ git+https://github.com/mediacloud/system-dev-ops@mc-deploy-0.4.2#subdirectory=deploy/mc-deploy
