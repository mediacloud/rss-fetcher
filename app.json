{
  "name": "Media Cloud Backup RSS Fetcher",
  "description": "Grab RSS feeds, parse out URLs, stick URLs in a daily synthetic RSS",
  "scripts": {
    "dokku": {
      "predeploy": "./install.sh",
      "postdeploy": "./postdeploy.sh"
    }
  }
}
